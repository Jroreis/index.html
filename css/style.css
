:root {
    --bg-color: #070707;
    --card-bg: #1A1A1A;
    --primary: #FFC107;
    --primary-dark: #b38600;
    --text-light: #E0E0E0;
}
* { box-sizing: border-box; margin: 0; padding: 0; font-family: 'Segoe UI', Roboto, Helvetica, Arial, sans-serif; }
body { background-color: var(--bg-color); color: var(--text-light); overflow-x: hidden; }

/* Navbar */
.navbar { display: flex; align-items: center; justify-content: space-between; background-color: #111; padding: 15px 20px; box-shadow: 0 4px 6px rgba(0,0,0,0.5); position: sticky; top: 0; z-index: 100; }
.navbar h1 { font-size: 18px; color: var(--primary); }
.navbar button { background: none; border: none; color: var(--primary); font-size: 20px; cursor: pointer; }

/* Drawer */
.drawer { position: fixed; top: 0; left: -300px; width: 280px; height: 100%; background-color: #111; z-index: 200; transition: 0.3s; box-shadow: 2px 0 10px rgba(0,0,0,0.5); }
.drawer.open { left: 0; }
.drawer-header { background-color: var(--primary); color: #000; padding: 30px 20px; text-align: center; }
.drawer-header h2 { font-size: 18px; margin-top: 10px; }
.drawer-header p { font-size: 12px; font-weight: bold; }
.drawer-menu { list-style: none; padding: 20px 0; }
.drawer-menu li { padding: 15px 20px; font-size: 16px; border-bottom: 1px solid #222; cursor: pointer; }
.drawer-menu li i { color: var(--primary); margin-right: 10px; width: 20px; }
.overlay { position: fixed; top:0; left:0; width:100%; height:100%; background: rgba(0,0,0,0.7); z-index: 150; display: none; }

/* Main Content & Views */
main { padding: 20px; padding-bottom: 80px; }
.view { display: none; animation: fadeIn 0.3s ease; }
.view.active { display: block; }
@keyframes fadeIn { from { opacity: 0; transform: translateY(10px); } to { opacity: 1; transform: translateY(0); } }

/* Grid Dashboard */
.grid-container { display: grid; grid-template-columns: repeat(auto-fit, minmax(140px, 1fr)); gap: 15px; }
.card { background-color: var(--card-bg); border-radius: 12px; padding: 25px 15px; text-align: center; cursor: pointer; border: 1px solid #333; transition: 0.2s; }
.card:active { transform: scale(0.95); border-color: var(--primary); }
.card-icon { font-size: 40px; color: var(--primary); margin-bottom: 15px; }
.card h3 { font-size: 15px; margin-bottom: 5px; }
.card p { font-size: 11px; color: #888; }

/* Forms & Tabs */
h2 { font-size: 20px; color: #fff; margin-bottom: 15px; text-align: center; border-bottom: 2px solid var(--primary); padding-bottom: 10px;}
.tabs { display: flex; margin-bottom: 20px; border-bottom: 1px solid #333; }
.tab { flex: 1; background: none; border: none; color: #888; padding: 10px; font-size: 14px; cursor: pointer; transition: 0.3s; }
.tab.active { color: var(--primary); border-bottom: 3px solid var(--primary); font-weight: bold; }
.tab-content { display: none; }
.tab-content.active { display: block; }

input { width: 100%; padding: 15px; margin-bottom: 15px; border-radius: 8px; border: 1px solid #333; background-color: #222; color: #fff; font-size: 16px; }
input:focus { outline: none; border-color: var(--primary); }
.form-group label { display: block; margin-bottom: 5px; color: var(--primary); font-size: 12px; }

.btn-primary, .btn-pdf { width: 100%; padding: 16px; border-radius: 8px; border: none; font-size: 16px; font-weight: bold; cursor: pointer; }
.btn-primary { background-color: var(--primary); color: #000; }
.btn-primary:active { background-color: var(--primary-dark); }
.btn-pdf { background-color: #d32f2f; color: #fff; margin-top: 15px; }

/* Resultado Box */
#resultado-container { margin-top: 30px; }
#resultado-box { background-color: #111; border-left: 4px solid #00E676; padding: 15px; border-radius: 5px; font-size: 14px; }
.res-row { margin-bottom: 8px; }
.res-label { color: #888; font-size: 12px; display:block; }
.res-val { color: #00E676; font-weight: bold; }
